# 左右分割の親機（左）として動作させる
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

# Bluetoothを強制的に有効化
CONFIG_ZMK_BLE=y

# 左右の通信とPC接続を安定させる最新の設定
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# スリープ機能を一旦オフにして接続を優先（安定したら後でオンにできます）
CONFIG_ZMK_SLEEP=n
