on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: ".github/workflows/build.yml"
      config_path: "config"

# --- ここから下が「何をビルドするか」の具体的な指示です ---
      build_matrix: |
        {
          "include": [
            { "board": "seeeduino_xiao_ble", "shield": "k_grid_left" },
            { "board": "seeeduino_xiao_ble", "shield": "k_grid_right" }
          ]
        }
