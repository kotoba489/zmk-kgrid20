name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk-config-workflow/.github/workflows/build-summary.yml@main
    with:
      board: seeeduino_xiao_ble
      shield: totem_left totem_right
      config_path: config
