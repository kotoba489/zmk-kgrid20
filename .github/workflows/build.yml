name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix: |
        [
          { "board": "seeeduino_xiao_ble", "shield": "k_grid_left" },
          { "board": "seeeduino_xiao_ble", "shield": "k_grid_right" }
        ]
